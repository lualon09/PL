// Si se quiere probar debe descomentarse antes del main.js el await(2).

main()-> Int {
    // Ambos show deberán imprimir los valores que el usuario introduzca por teclado.
    Int a = read();
    show(a);
    Bool b = read();
    show(b);
    return 0;
}